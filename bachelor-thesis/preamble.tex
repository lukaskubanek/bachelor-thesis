%%%%%%%%%%%%
% Preamble %
%%%%%%%%%%%%

% document class
\documentclass[
    ngerman,
    toc=listof,
    toc=bibliography,
    subjectthesis=true,
    % comment out the following line to produce
    % the print version of the thesis
    cdtitle=color
]{tudscrreprt}

% encoding & language
\usepackage{selinput}
\SelectInputMappings{}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}
\usepackage[babel, german=quotes]{csquotes}
\usepackage{isodate}

% spacing
\usepackage{parskip}
\usepackage{setspace}
\onehalfspacing
\recalctypearea

% numbering depth
\setcounter{secnumdepth}{3}

% listings
\usepackage{listings}
\usepackage{scrhack}
\lstset{
    backgroundcolor=\color{gray!10},
    basicstyle=\small,
    tabsize=4,
    captionpos=b,
    aboveskip=15pt,
    belowskip=10pt
}

% URLs
\usepackage[hyphens]{url}
\usepackage{hyperref}

% figures
\usepackage{graphicx}
\usepackage[labelformat=simple]{subcaption}
\renewcommand\thesubfigure{(\alph{subfigure})}

% footnotes
\interfootnotelinepenalty=10000
\usepackage{chngcntr}
\counterwithout{footnote}{chapter}

% enumerations
\usepackage{enumitem}

% bibliography
\usepackage[numbers, square]{natbib}

% testing
\usepackage{blindtext}
